#include <stdio.h>

/**
 * main - Prints a sentence for puts.c
 * Return:0 if exited properly, otherwise a non-zero value
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
